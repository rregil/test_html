version: 0.1
component: build
timeoutInSeconds: 10000
shell: bash
failImmediatelyOnError: true
env:
  variables:
    key: "value"
  exportedVariables:
    - outputVariable

steps:
  - type: Command
    name: Check environment
    shell: bash
    timeoutInSeconds: 650
    failImmediatelyOnError: true
    command: ls
